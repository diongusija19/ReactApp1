# ReactApp1

A React + TypeScript focus session tracker.

## Features

- Start a session by entering a task and clicking `Start`
- Live timer that counts elapsed seconds
- Stop a session with `Stop` and automatically log the completed entry
- View completed sessions with task name and duration in seconds

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
